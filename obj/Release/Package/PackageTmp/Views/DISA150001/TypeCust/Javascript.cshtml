
<script>
    var SITE_URL = window.location.href + "/";
    var Message = [];

    var GET_SITE_URL = (window.location.href).split('/');
    var NEW_SITE_URL = "";

    for (var i = 0; i < GET_SITE_URL.length; i++) {
        if (GET_SITE_URL[i] != "DISA150001") {
            NEW_SITE_URL = NEW_SITE_URL + GET_SITE_URL[i] + "/";
        }
    }

    function enterKey_Press() {
        $('input').bind("enterKey", function (e) {

            OnSearch(1);

        });
        $('input').keyup(function (e) {
            if (e.keyCode == 13) {
                $(this).trigger("enterKey");
            }
        });
    }

    $(document).ready(function () {
        enterKey_Press();
        jQuery('#btn_search_typeCust').click();
        $('#btn_Delete').prop('disabled', true);
        $('#btn_edit').prop('disabled', true);
        $('#btn_clear').prop('disabled', true);

        $('.select2').select2({ width: '100%' });        

        $('#addForm').submit(function (e) {
            e.preventDefault();
            addNew();
        });

        $('#editForm').submit(function (e) {
            e.preventDefault();
            updateData();
        });

        $('#searchStatus').on('change', function () {
            if (this.value == 'Other') {
                $('#otherStatus').show();
            } else {
                $('#otherStatus').hide();
            }
        });

        $.ajax({
            url: "Message/GenerateMessage",
            success: function (e) {
                Message = e.res;

                for (var i = 0; i < Message.length; i++) {
                    var MSG_ID = Message[i].MSG_ID;
                    if (MSG_ID == "MWP005") {
                        $('#modal-save-tittle').text(Message[i].MSG_TEXT);
                    } else if (MSG_ID == "MWP006") {
                        $('#modal-delete-tittle').text(Message[i].MSG_TEXT);
                    } else if (MSG_ID == "MWP007") {
                        $('#modal-update-tittle').text(Message[i].MSG_TEXT);
                    }
                }
            }
        });
    });

    function GenerateMessage(M, Text) {
        for (var i = 0; i < Message.length; i++) {
            var MSG_ID = Message[i].MSG_ID;
            var MSG_TXT = (Message[i].MSG_TEXT).split('{0}');
            if (MSG_ID == Text) {
                var NEW_TXT = "";
                for (var e = 0; e < MSG_TXT.length; e++) {
                    if (e < (MSG_TXT.length - 1)) {
                        if (Array.isArray(M)) {
                            NEW_TXT = NEW_TXT + MSG_TXT[e] + " " + M[e];
                        } else {
                            NEW_TXT = NEW_TXT + MSG_TXT[e] + " " + M;
                        }
                        
                    } else {
                        NEW_TXT = NEW_TXT + " " + MSG_TXT[e];
                    }
                }
                if (Text == 'MWP001' || Text == 'MWP003') {
                    toastr.success(NEW_TXT);
                } else if (Text == "MWP011" || Text == "MWP013" || Text == "MWP016" || Text == "MWP017") {
                    toastr.error(NEW_TXT);
                } else {
                    toastr.warning(NEW_TXT);
                }                
            }
        }
    }

    function add_check() {              
        var ValidationState = true;
        var ValidationMessage = "";

        var ProyekData = {
            Dmc_Type: $('#addDmcType').val(),
            Customer: $('#addCustomer').val(),
            Touch_Panel_Size: $('#addTouchPanelSize').val(),
            Wis_Version: $('#addWisVersion').val(),
            Lot_Size: $('#addLotSize').val(),
            In_Direct: $('#addInDirect').val(),
            Sga: $('#addSga').val()
        }

        if (ProyekData.Dmc_Type == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert DMC TYPE!"
        }
        else if (ProyekData.Customer == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert CUSTOMER!"
        }
        else if (ProyekData.Touch_Panel_Size == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert TOUCH PANEL SIZE!"
        }
        else if (ProyekData.Wis_Version == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert WIS VERSION!"
        }
        else if (ProyekData.Lot_Size == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert LOT SIZE!"
        }
        else if (ProyekData.In_Direct== "") {
            ValidationState = false;
            ValidationMessage = "Please Insert INDIRECT!"
        }
        else if (ProyekData.SGA == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert SGA!"
        }

        if (ValidationState != true) {
            toastr.error(ValidationMessage);
            return;
        }
         else {
            $('#PopupForm_AddNewConfirm').modal();
        }
           
    }

    function edit_check() {
        var ValidationState = true;
        var ValidationMessage = "";

        var ProyekData = {
            Dmc_Type: $('#editDmcType').val(),
            Customer: $('#editCustomer').val(),
            Touch_Panel_Size: $('#editTouchPanelSize').val(),
            Wis_Version: $('#editWisVersion').val(),
            Lot_Size: $('#editLotSize').val()
        }

        if (ProyekData.Dmc_Type == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert DMC TYPE!"
        }
        else if (ProyekData.Customer == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert CUSTOMER!"
        }
        else if (ProyekData.Touch_Panel_Size == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert TOUCH PANEL SIZE!"
        }
        else if (ProyekData.Wis_Version == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert WIS VERSION!"
        }
        else if (ProyekData.Lot_Size == "") {
            ValidationState = false;
            ValidationMessage = "Please Insert LOT SIZE!"
        }

        if (ValidationState != true) {
            toastr.error(ValidationMessage);
            return;
        }
        else {
            $('#PopupForm_EditConfirm').modal();
        }
    }

    $('#searchStatus').on('click', function () {

    });

    // tombol Add Confirm
    function addDataConfirm() {
        /*$('#PopupForm_AddNewConfirm').modal();*/
        add_check();
    }

    
    //tombol Add New
    function addNew() {       
        var Dmc_Type = $('#addDmcType').val();
        var Customer = $('#addCustomer').val();
        var Touch_Panel_Size = $('#addTouchPanelSize').val();
        var Wis_Version = $('#addWisVersion').val();
        var Lot_Size = $('#addLotSize').val();
        var In_Direct = $('#addInDirect').val();
        var Sga = $('#addSga').val();
       

        $.ajax({
            type: "POST",
            url: SITE_URL + '../ADD_NEW_TypeCust',
            async: false,
            data: {
                Dmc_Type: Dmc_Type,
                Customer: Customer,
                Touch_Panel_Size: Touch_Panel_Size,
                Wis_Version: Wis_Version,
                Lot_Size: Lot_Size,
                In_Direct: In_Direct,
                Sga: Sga
            },
            success: function (res) {
                if (res.sts == "TRUE") {
                    toastr.success("Data Has Been Save Successfuly");
                    //searchData();
                    //clear_add();                    
                    $('#modalForm').modal('hide');
                    $('#btncloselookup').click();
                    $('#btnNo').click();
                    OnSearch(1);
                } else {
                    toastr.warning(res.message);
                }
            },
            error: function (res) {
                toastr.error(res.Message);
            }
        });
        clear_add();
        location.reload();
        jQuery('#btn_search_typeCust').click();
    }

    function clear_add() {
        $('input[type="text"]').val('');
        $('.form-control').val('');
        $('.form-control select2').val('');
        $('.select2').val('');

        var dmc_type = $('#addDmcType').val('');
        $('#addCustomer').val('');
        $('#addTouchPanelSize').val('');
        $('#addWisVersion').val('');
        $('#addLotSize').val('');
        //var In_Direct = $('#addInDirect').val('');
        //var Sga = $('#addSga').val('');
       
    }

     //tombol Edit
    $('#btn_edit').on('click', function () {
        var ID = $('#EditID');
        var Dmc_Type = $('#editDmcType');
        var Customer = $('#editCustomer');        
        var Touch_Panel_Size = $('#editTouchPanelSize');
        var Wis_Version = $('#editWisVersion');
        var Lot_Size = $('#editLotSize');
        var In_Direct = $('#editInDirect');
        var Sga = $('#editSga');
        var Var = "";
        var Send_Var = "";
        var Send_Var = "";
        
        $("input[name='checkdata[]']").each(function () {
            if ($(this).is(':checked')) {
                Var = "";
                Var = Var + $(this).val();
                Send_Var = Var.split(',');
            }
        });

        ID.val(Send_Var[0]);
        Dmc_Type.val(Send_Var[1]);
        Customer.val(Send_Var[2]);
        Touch_Panel_Size.val(Send_Var[3]);
        Wis_Version.val(Send_Var[4]);
        Lot_Size.val(Send_Var[5]);
        In_Direct.val(Send_Var[6]);
        Sga.val(Send_Var[7]);        

       
        $('#modalEditForm').modal();

    });

    function cancel_edit() {
        var ID = $('#EditID').val();
        $('#edit_data').hide();
        $('.row-' + ID).show();
    }

    //EditDataConfirm
    function EditDataConfirm() {
        /*$('#PopupForm_EditConfirm').modal();*/
        edit_check();
    }

    function updateData() {
        $("input[name='checkdata[]']").each(function () {
            if ($(this).is(':checked')) {
                var ID = $('#EditID').val();
                var Dmc_Type = $('#editDmcType').val();
                var Customer = $('#editCustomer').val();
                var Touch_Panel_Size = $('#editTouchPanelSize').val();
                var Wis_Version = $('#editWisVersion').val();
                var Lot_Size = $('#editLotSize').val();
                var In_Direct = $('#editInDirect').val();
                var Sga = $('#editSga').val();                
                var Send_Var = ID + "," + Dmc_Type + "," + Customer + "," + Touch_Panel_Size + "," + Wis_Version + "," + Lot_Size + "," + In_Direct + "," + Sga;

                $.ajax({
                    type: "POST",
                    url: SITE_URL + '../Update_Data_TypeCust',
                    async: false,
                    data: {
                        DATA: Send_Var
                    },
                    success: function (data) {
                        if (data.sts == "TRUE") {
                            toastr.success(data.message);
                            cancel_edit();                        
                            $('#btn_edit').attr('disabled', true);
                            $('#modalEditForm').modal('hide');
                        }
                        else {
                            toastr.warning(data.message);
                        }
                        $("#PopupForm_EditConfirm").modal('hide');

                        OnSearch(1);
                    }
                });
            }
        });

    }

    //tombol Delete
    $("#btn_Delete").on("click", function () {
        $('#Confirmation_Delete_Popup').modal();
    });

    function delete_data() {
        var chk = $("input[name='checkdata[]']:checked").length;
        //alert('hai >> ' + chk);

        if (chk == 0) {
            GenerateMessage("", "MWP012");
        }
        else {
            var Var = "";
            var Send_Var = "";
            $("input[name='checkdata[]']").each(function () {
                if ($(this).is(':checked')) {
                    Var = "";
                    Var = Var + $(this).val();
                    if (Var != '') {
                        Send_Var = Send_Var + Var.split(',')[0] + ",";
                    } else {
                        Send_Var = Send_Var + "," + Var.split(',')[0];
                    }

                }
            });

            $.ajax({
                type: "POST",
                url: SITE_URL + '../Delete_Data_TypeCust',
                async: false,
                data: {
                    DATA: Send_Var
                },
                success: function (data) {
                    if (data.sts == "TRUE") {
                        toastr.success(data.message);
                    }
                    else {
                        toastr.warning(data.message);
                    }
                    $("#Confirmation_Delete_Popup").modal('hide');

                    OnSearch(1);
                    //xxx
                }
            });
        }
    }

   
 

    // tombol new
    $("#btn_new").on("click", function () {        
        $('#addDmcType').val();
        $('#addCustomer').val();
        $('#addTouchPanelSize').val();
        $('#addWisVersion').val();
        $('#addLotSize').val();
        $('#addInDirect').val();
        $('#addSga').val();
        $('#modalForm').modal();

        $("#btncloselookup").on("click", function () {
            $('#tambah_data').hide();
        });
    });

    //tombol clear
    function clear_search_typeCust() {
        $('#searchDmcType').val('');
        $('#searchCustomer').val('');
        $('#searchLotSize').val('');
        $.ajax({            
            type: "POST",
            url: SITE_URL + '../Search_Data_TypeCust',
            async: false,
            data: {
                start: 1,
                display: 10,
                DATA_ID: $("#data_nm").val(),
                EXECUTION_TIME: $("#exec_time_cd").val(),
                TIME_UNIT_CRITERIA: $("#time_unit_cd").val(),
                STATUS_ACTIVE: $("#sts_cd").val(),
                EMPLOYEE_NAME: "xxxxx",
            },
            success: function (data) {
                $("#GridTable").html(data);
                $("#paging").val(Last_Record_Per_Page);
                if ($('#cntPage').val() == "1") {
                    $('#btn_clear').prop('disabled', false);
                    $('#btn_edit').prop('disabled', true);
                    $('#btn_Delete').prop('disabled', true);
                } else {
                    $('#btn_clear').prop('disabled', false);
                    $('#btn_edit').prop('disabled', true);
                    $('#btn_Delete').prop('disabled', true);
                }

                scroll_and_checkbox();
                cek_perdata();
            }
        });
    }


    // tombol search
    $("#btn_search_typeCust").on("click", function () {
        OnSearch(1);
    });

    // tombol pencarian data
    function OnSearch(page) {
        searchData(page, $('#paging').val());
        $('#btn_download').prop('disabled', false);
        $('#btn_clear').prop('disabled', false);
    }

    // proses pencarian data
    function searchData(page, size) {
        var Dmc_Type = $('#searchDmcType').val();
        var Customer = $('#searchCustomer').val();
        var Lot_Size = $('#searchLotSize').val();
        
        Last_Record_Per_Page = $("#paging").val();
        $.ajax({            
            type: "POST",
            url: SITE_URL + '../Search_Data_TypeCust',
            async: false,
            data: {
                start: page,
                display: size,
                DATA_ID: $("#data_nm").val(),               
                Dmc_Type: Dmc_Type,
                Customer: Customer,
                Lot_Size: Lot_Size
            },
            success: function (data) {
                $("#GridTable").html(data);
                $("#paging").val(Last_Record_Per_Page);
                if ($('#cntPage').val() == "1") {
                    $('#btn_clear').prop('disabled', false);
                } else {
                    $('#btn_clear').prop('disabled', false);
                }

                scroll_and_checkbox();
                cek_perdata();
            }
        });
    }


    // ceklist per data
    function cek_perdata() {
        $("input[name='checkdata[]']").on("click", function () {
            if ($(this).is(':checked')) {
                $('#btn_Delete').prop('disabled', false);
                total_check = $('.grid-checkbox:checkbox:checked').length;
                if (total_check == 1) {
                    $('#btn_edit').prop('disabled', false);
                } else {
                    $('#btn_edit').prop('disabled', true);
                }

                Var = "";

                $("input[name='checkdata[]']").each(function () {
                    if ($(this).is(':checked')) {
                        Var = Var + $(this).val();
                    }
                });

            }
            else {
                total_check = $('.grid-checkbox:checkbox:checked').length;
                if (total_check == 0) {
                    $('#btn_Delete').prop('disabled', true);
                    $('#btn_edit').prop('disabled', true);
                } else if (total_check == 1) {
                    $('#btn_edit').prop('disabled', false);
                    $('#btn_Delete').prop('disabled', false);
                }


                $("input[name='time_unit_data[]']").prop('disabled', true);
                $("input[name='time_value_data[]']").prop('disabled', true);
                $("input[name='exec_time_data[]']").prop('disabled', true);
                $("input[name='status_data[]']").prop('disabled', true);

                $("button[name='btnConfirmEditData[]']").prop('disabled', true);
                $("button[name='btncloselookup[]']").prop('disabled', true);
            }
        });
    }


    // ceklist all data dan scroll
    function scroll_and_checkbox() {
        $("#tScrollBody").scroll(function () {
            $("#tScrollHead").scrollLeft($("#tScrollBody").scrollLeft());
        });


        $("#checkall").click(function () {
            if ($("#checkall").is(":checked")) {
                $(".grid-checkbox").prop('checked', true);
                $('#btn_Delete').prop('disabled', false);
                $('#btn_edit').prop('disabled', true);
            }
            else {
                $(".grid-checkbox").prop('checked', false);
                $('#btn_Delete').prop('disabled', true);
                $('#btn_edit').prop('disabled', true);
            }
        });
    }

 


</script>