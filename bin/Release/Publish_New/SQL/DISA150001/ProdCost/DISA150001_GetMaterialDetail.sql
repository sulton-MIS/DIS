--SELECT TOP(1)
--	NAME as MATERIAL_NAME,
--	XTANK.PRICE as UNIT_PRICE,
--	XHEAD.TANI1 as UNIT,
--	XTANK.EDATE
--FROM XHEAD 	
--LEFT OUTER JOIN XTANK ON XHEAD.CODE = XTANK.CODE
--WHERE XHEAD.CODE = @MATERIAL_KODE and XTANK.EDATE in (999999991,999999999)
----AND (XHEAD.NAME not like '%(DONOTUSE)%')
--ORDER BY XTANK.INPUTDATE DESC

SELECT TOP(1)
	NAME as MATERIAL_NAME,	
	TANI1 as UNIT
FROM XHEAD 	
WHERE CODE = @MATERIAL_KODE 
--AND (XHEAD.NAME not like '%(DONOTUSE)%')
