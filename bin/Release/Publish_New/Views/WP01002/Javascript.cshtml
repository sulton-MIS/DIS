
<script>
    var SITE_URL = window.location.href+"/";
    var Message = [];
    var GET_SITE_URL = (window.location.href).split('/');
        var NEW_SITE_URL = "";

        for (var i = 0; i < GET_SITE_URL.length; i++) {
            if (GET_SITE_URL[i] != "WP01002") {
                NEW_SITE_URL = NEW_SITE_URL + GET_SITE_URL[i] + "/";
            }
    }
    
    $(document).ready(function () {
        $('#addLabelMaxUpload').text(uploadSize + 'KB');
        $('#editLabelMaxUpload').text(uploadSize + 'KB');
        $('#btn_Delete').prop('disabled', true);
        $('#btn_edit').prop('disabled', true);
        $('#btn_clear').prop('disabled', true);

        $('#searchStatus').on('change', function () {
            if (this.value == 'Other') {
                $('#otherStatus').show();
            } else {
                $('#otherStatus').hide();
            }
        });

        
        $.ajax({
            url: NEW_SITE_URL + "Message/GenerateMessage",
            success: function (e) {
                Message = e.res;

                for (var i = 0; i < Message.length; i++) {
                    var MSG_ID = Message[i].MSG_ID;
                    if (MSG_ID == "MWP005") {
                        $('#modal-save-tittle').text(Message[i].MSG_TEXT);
                    } else if (MSG_ID == "MWP006") {
                        $('#modal-delete-tittle').text(Message[i].MSG_TEXT);
                    } else if (MSG_ID == "MWP007") {
                        $('#modal-update-tittle').text(Message[i].MSG_TEXT);
                    }
                }
            }
        });
    });

    function change_image(type, input) {
        if (type == 'add') {
            if (input.files && input.files[0]){
	            var reader = new FileReader();
	            reader.onload = function(e){
	              $('#addImageBox').attr('src', e.target.result).show();
                }

	            $('.plus-image').css('display', "none");
	            reader.readAsDataURL(input.files[0]);
	        }else{
	  	        $('.plus-image').css('display', "block");
	        }
        } else {
            if (input.files && input.files[0]){
	            var reader = new FileReader();
	            reader.onload = function(e){
	              $('#editImageBox').attr('src', e.target.result).show();
                }
                $('#editFoto').show();
                $('#editFakeFoto').hide();
                $('#editFileName').hide();
	            
	            reader.readAsDataURL(input.files[0]);
	        }
        }
        
    }

     function GenerateMessage(M, Text) {
        for (var i = 0; i < Message.length; i++) {
            var MSG_ID = Message[i].MSG_ID;
            var MSG_TXT = (Message[i].MSG_TEXT).split('{0}');
            if (MSG_ID == Text) {
                var NEW_TXT = "";
                for (var e = 0; e < MSG_TXT.length; e++) {
                    if (e < (MSG_TXT.length - 1)) {
                        if (Array.isArray(M)) {
                            NEW_TXT = NEW_TXT + MSG_TXT[e] + " " + M[e];
                        } else {
                            NEW_TXT = NEW_TXT + MSG_TXT[e] + " " + M;
                        }
                        
                    } else {
                        NEW_TXT = NEW_TXT + " " + MSG_TXT[e];
                    }
                }
                if (Text == 'MWP001' || Text == 'MWP002' || Text == 'MWP003') {
                    toastr.success(NEW_TXT);
                } else if (Text == "MWP011" || Text == "MWP013" || Text == "MWP016" || Text == "MWP017") {
                    toastr.error(NEW_TXT);
                } else {
                    toastr.warning(NEW_TXT);
                }
                
            }
        }
    }

    $('#searchStatus').on('click', function () {

    });

    // tombol Add Confirm
    function addDataConfirm() {
        $('#PopupForm_AddNewConfirm').modal();
    }

    function add_check() {
        var AREA_CODE = $('#addAreaCode').val();
        var AREA_NAME = $('#addAreaName').val();
        var AREA_STATUS = $('#addAreaStatus').val();
        var FOTO = '';
        var status = true;
        if (AREA_CODE == null || AREA_CODE == "") {
            GenerateMessage("AREA CODE", "MWP008");
            status = false;
        }

        if (AREA_NAME == null || AREA_NAME == "") {
            GenerateMessage("AREA NAME", "MWP008");
            status = false;
        }

        if ($('#addFoto').val() != "") {
            var GET_FOTO_NAME = ($('#addFoto')[0].files[0].name).split('.');
            var GET_FOTO_SIZE = $('#addFoto')[0].files[0].size;
            FOTO = (GET_FOTO_NAME[GET_FOTO_NAME.length - 1]).toUpperCase();
            
            if(FOTO != 'JPG' && FOTO != 'PNG' && FOTO != 'BMP' && FOTO != 'SVG' && FOTO != 'TIFF' && FOTO != 'JPEG' && FOTO != 'GIF'){
                GenerateMessage([ FOTO, 'Image' ], "MWP011");
                status = false;
            }

            if (GET_FOTO_SIZE > (uploadSize * 1000 )) {
                toastr.warning('File Upload Cannot Exced '+uploadSize+'KB');
                status = false;
            }
        }

        
        //if (FOTO == null || FOTO == "") {
        //    GenerateMessage("FOTO", "MWP008");
        //    status = false;
        //}

        if (status == false) {
            return;
        } else {
            $('#PopupForm_AddNewConfirm').modal();
        }
    }

    function edit_check() {
        var AREA_CODE = $('#editAreaCode').val();
        var AREA_NAME = $('#editAreaName').val();
        var AREA_STATUS = $('#editAreaStatus').val();
        var FOTO = $('#editImageBox').attr('src');
        
        var status = true;

        if (AREA_CODE == null || AREA_CODE == "") {
            GenerateMessage("AREA CODE", "MWP008");
            status = false;
        }

        if (AREA_NAME == null || AREA_NAME == "") {
            GenerateMessage("AREA NAME", "MWP008");
            status = false;
        }

        $('.editImageBox').each(function() {
        if (!this.complete || typeof this.naturalWidth == "undefined" || this.naturalWidth == 0) {
            $(this).attr('src', '');
        }
        });

        if ($('#editFoto').val() != "") {
            var GET_FOTO_NAME = ($('#editFoto')[0].files[0].name).split('.');
            var GET_FOTO_SIZE = $('#editFoto')[0].files[0].size;
            FOTO = (GET_FOTO_NAME[GET_FOTO_NAME.length - 1]).toUpperCase();
            
            if(FOTO != 'JPG' && FOTO != 'PNG' && FOTO != 'BMP' && FOTO != 'SVG' && FOTO != 'TIFF' && FOTO != 'JPEG' && FOTO != 'GIF'){
                GenerateMessage([ FOTO, 'Image' ], "MWP011");
                status = false;
            }

            if (GET_FOTO_SIZE > (uploadSize * 1000 )) {
                toastr.warning('File Upload Cannot Exced ' + uploadSize + 'KB');
                status = false;
            }
        }

        
        //if (FOTO == null || FOTO == "") {
        //    GenerateMessage("FOTO", "MWP008");
        //    status = false;
        //}

        if (status == false) {
            return;
        } else {
            $('#PopupForm_EditConfirm').modal();
        }
    }

    function MoveFoto(foto, area) {
        var reader = new FileReader();
        var myFormData = new FormData();
        myFormData.append('ImagePath', $(foto)[0].files[0]);
        myFormData.append('AREA_CD', area);
        $.ajax({
            url: SITE_URL + "MoveFile",
            type: 'POST',
			processData : false,
			contentType: false,
			data : myFormData,
            success: function (e) {
                console.log(e);
            }
        });
    }

    //tombol Add New
    function addNew() {
        var AREA_CODE = $('#addAreaCode').val();
        var AREA_NAME = $('#addAreaName').val();
        var AREA_STATUS = $('#addAreaStatus').val();
        var EXT = $('#addFoto').val().substr(($('#addFoto').val().lastIndexOf('.') + 1));
        if ($('#addFoto').val() != "") {
            var GET_FOTO_NAME = ($('#addFoto')[0].files[0].name).split('.');
            FOTO = (GET_FOTO_NAME[GET_FOTO_NAME.length-1]).toUpperCase();
        }

        $.ajax({
            type: "POST",
            url: SITE_URL + 'ADD_NEW',
            async: false,
            data: {
                AREA_CODE: AREA_CODE,
                AREA_NAME: AREA_NAME,
                AREA_STATUS: AREA_STATUS,
                EXT: EXT
            },
            success: function (res) {
                if (res.sts == "TRUE") {
                    toastr.success(res.message);
                    MoveFoto('#addFoto', AREA_CODE);
                    searchData();
                    clear_add();
                    $('#modalForm').modal('hide');
                    $('#btncloselookup').click();
                } else {
                    toastr.warning(res.message);
                }
            },
            error: function (res) {
                toastr.error(res.Message);
            }
        });
    }

    function clear_add() {
        var AREA_CODE = $('#addAreaCode').val('');
        var AREA_NAME = $('#addAreaName').val('');
        var AREA_STATUS = $('#addAreaStatus').val(1);
        var IMG_AREA = $('#addImageBox').attr('src', '');
        var IMG_FILE = $('#addFoto').val('');
    }

    //tombol Edit

    $('#btn_edit').on('click', function () {
        var ID = $('#EditID');
        var AREA_CODE = $('#editAreaCode');
        var AREA_NAME = $('#editAreaName');
        var AREA_ST = $('#editAreaStatus');
        var IMAGE_FILE = $('#editImageBox');
        var Var = "";
        var Send_Var = "";
        var Send_Var = "";
        $("input[name='checkdata[]']").each(function () {
            if ($(this).is(':checked')) {
                Var = "";
                Var = Var + $(this).val();
                Send_Var = Var.split(',');
            }
        });
        ID.val(Send_Var[0]);
        AREA_CODE.val(Send_Var[1]);
        AREA_NAME.val(Send_Var[2]);
        AREA_ST.val(Send_Var[3]);

        if (Send_Var[4] != null || Send_Var[4] != '') {
            IMAGE_FILE.attr('src', Send_Var[4]);
            $('#editFakeFoto').show();
            $('#editFoto').hide();
            $('#editFileName').text(Send_Var[4]).show();
            $('#editImageBox').show();
        }
        
        $('#modalEditForm').modal();
    });

    function cancel_edit() {
        var ID = $('#EditID').val();
        $('#edit_data').hide();
        $('.row-' + ID).show();
        $('#editFoto').val('');
    }

    //EditDataConfirm
    function EditDataConfirm() {
        $('#PopupForm_EditConfirm').modal();
    }

    function updateData() {
        $("input[name='checkdata[]']").each(function () {
            if ($(this).is(':checked')) {
                var ID = $('#EditID').val();
                var AREA_CODE = $('#editAreaCode').val();
                var AREA_NAME = $('#editAreaName').val();
                var AREA_ST = $('#editAreaStatus').val();
                var EXT = $('#editFoto').val().substr(($('#editFoto').val().lastIndexOf('.') + 1));
                var Send_Var = ID+","+AREA_CODE+","+AREA_NAME+","+AREA_ST+","+EXT;

                 $.ajax({
                    type: "POST",
                    url: SITE_URL + 'Update_Data',
                    async: false,
                    data: {
                        DATA: Send_Var
                    },
                     success: function (data) {
                        if (data.sts == "TRUE") {
                            if ($('#editFoto').val() != '') {
                                MoveFoto('#editFoto', AREA_CODE);
                            }
                            GenerateMessage('Area', 'MWP003');
                            cancel_edit();
                            $('#btn_edit').attr('disabled', true);
                            $('#modalEditForm').modal('hide');
                            
                        }
                        else {
                            toastr.error(data.message);
                        }
                        $("#PopupForm_EditConfirm").modal('hide');
                    
                        OnSearch(1);
                        //xxx
                    }
                });
            }
        });

    }

    //tombol Delete
    $("#btn_Delete").on("click", function () {
        $('#Confirmation_Delete_Popup').modal();
    });

    function delete_data() {
        var chk = $("input[name='checkdata[]']:checked").length;
        //alert('hai >> ' + chk);

        if (chk == 0) {
            GenerateMessage("", "MWP012");
        }
        else {
            var Var = "";
            var Send_Var = "";
            var Delete_Name = "";
            $("input[name='checkdata[]']").each(function () {
                if ($(this).is(':checked')) {
                    Var = "";
                    Var = Var + $(this).val();
                    if (Var != '') {
                        Send_Var = Send_Var + Var.split(',')[0] + ",";
                        Delete_Name = Delete_Name + Var.split(',')[1] + ",";
                    } else {
                        Send_Var = Send_Var + "," + Var.split(',')[0];
                        Delete_Name = Delete_Name + "," + Var.split(',')[1];
                    }
                    
                }
            });

            $.ajax({
                type: "POST",
                url: 'WP01002/Delete_Data',
                async: false,
                data: {
                    DATA: Send_Var
                },
                success: function (data) {
                    var DELETED_NAME = Delete_Name.split(',');
                    for (var i = 0; i < data.DELETE_MSG.length; i++) {
                        var getData = data.DELETE_MSG[i];
                        if (getData.DELETE_MSG == 'True') {
                            GenerateMessage(DELETED_NAME[i], "MWP002");
                        } else {
                            GenerateMessage(DELETED_NAME[i], "MWP009");
                        }
                        
                    }
                    $("#Confirmation_Delete_Popup").modal('hide');
                    
                    OnSearch(1);
                    //xxx
                }
            });
        }
    }

    //tombol clear
    function clear_search() {
        $('#searchAreaCode').val("");
        $('#searchAreaName').val("");
        $.ajax({
            type: "POST",
            url: SITE_URL + 'Search_Data',
            async: false,
            data: {
                start: 1,
                display: 10,
                DATA_ID: $("#data_nm").val(),
                EXECUTION_TIME: $("#exec_time_cd").val(),
                TIME_UNIT_CRITERIA: $("#time_unit_cd").val(),
                STATUS_ACTIVE: $("#sts_cd").val(),
                AREA_CODE: "xxxxx",
                AREA_NAME: "xxxxx",
            },
            success: function (data) {
                $("#GridTable").html(data);
                $("#paging").val(Last_Record_Per_Page);
                if ($('#cntPage').val() == "1") {
                    $('#btn_clear').prop('disabled', false);
                    $('#btn_edit').prop('disabled', true);
                    $('#btn_Delete').prop('disabled', true);
                } else {
                    $('#btn_clear').prop('disabled', false);
                    $('#btn_edit').prop('disabled', true);
                    $('#btn_Delete').prop('disabled', true);
                }
                
                scroll_and_checkbox();
                cek_perdata();    
            }
        });
    }

    // tombol search
    $("#btn_search").on("click", function () {
        OnSearch(1);
    });

    // tombol new
    $("#btn_new").on("click", function ()
    {
        $('#addAreaCode').val('');
        $('#addAreaName').val('');
        $('#addAreaStatus').val(1);
        $('#modalForm').modal();
        $('#addImageBox').attr('src', '');
        $('#addFoto').val('');
        $("#btncloselookup").on("click", function () {
            $('#tambah_data').hide();
        });
    });

    // tombol pencarian data
    function OnSearch(page)
    {   
        searchData(page, $('#paging').val());

        $('#btn_download').prop('disabled', false);
        $('#btn_clear').prop('disabled', false);
    }

    // proses pencarian data
    function searchData(page, size) {
        var AREA_CODE = $('#searchAreaCode').val();
        var AREA_NAME = $('#searchAreaName').val();

        if (status == 'Other') {
            status = $('#otherStatus').val();
        }

        Last_Record_Per_Page = $("#paging").val();
        $.ajax({
            type: "POST",
            url: SITE_URL + 'Search_Data',
            async: false,
            data: {
                start: page,
                display: size,
                DATA_ID: $("#data_nm").val(),
                EXECUTION_TIME: $("#exec_time_cd").val(),
                TIME_UNIT_CRITERIA: $("#time_unit_cd").val(),
                STATUS_ACTIVE: $("#sts_cd").val(),
                AREA_CODE: AREA_CODE,
                AREA_NAME: AREA_NAME
            },
            success: function (data) {
                $("#GridTable").html(data);
                $("#paging").val(Last_Record_Per_Page);
                if ($('#cntPage').val() == "1") {
                    $('#btn_clear').prop('disabled', false);
                } else {
                    $('#btn_clear').prop('disabled', false);
                }
                
                scroll_and_checkbox();
                cek_perdata();    
            }
        });
    }

    // ceklist per data
    function cek_perdata()
    {
        $("input[name='checkdata[]']").on("click", function () {
            if ($(this).is(':checked')) {
                $('#btn_Delete').prop('disabled', false);
                total_check = $('.grid-checkbox:checkbox:checked').length;
                if (total_check == 1) {
                    $('#btn_edit').prop('disabled', false);
                } else {
                    $('#btn_edit').prop('disabled', true);
                }

                Var = "";

                $("input[name='checkdata[]']").each(function () {
                    if ($(this).is(':checked')) {
                        Var = Var + $(this).val();
                    }
                });

            }
            else {
                total_check = $('.grid-checkbox:checkbox:checked').length;
                if (total_check == 0) {
                    $('#btn_Delete').prop('disabled', true);
                    $('#btn_edit').prop('disabled', true);
                }else if (total_check == 1) {
                    $('#btn_edit').prop('disabled', false);
                    $('#btn_Delete').prop('disabled', false);
                }
                

                $("input[name='time_unit_data[]']").prop('disabled', true);
                $("input[name='time_value_data[]']").prop('disabled', true);
                $("input[name='exec_time_data[]']").prop('disabled', true);
                $("input[name='status_data[]']").prop('disabled', true);

                $("button[name='btnConfirmEditData[]']").prop('disabled', true);
                $("button[name='btncloselookup[]']").prop('disabled', true);
            }
        });
    }

    // ceklist all data dan scroll
    function scroll_and_checkbox() {
        $("#tScrollBody").scroll(function () {
            $("#tScrollHead").scrollLeft($("#tScrollBody").scrollLeft());
        });


        $("#checkall").click(function () {
            if ($("#checkall").is(":checked"))
            {
                $(".grid-checkbox").prop('checked', true);
                $('#btn_Delete').prop('disabled', false);
                $('#btn_edit').prop('disabled', true);
            }
            else
            {
                $(".grid-checkbox").prop('checked', false);
                $('#btn_Delete').prop('disabled', true);
                $('#btn_edit').prop('disabled', true);
            }
        });
    }
</script>