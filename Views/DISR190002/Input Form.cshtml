@using AI070.Models;
@using AI070.Models.Shared;
@using AI070.Models.DISR190002Master;
@using System.Data;
@{
    List<DISR190002Master> DataWP = (List<DISR190002Master>)ViewData["DataDISR190002"];
    List<DISR190002Similar_Type> List_Tipe_Similar = (List<DISR190002Similar_Type>)ViewData["DataSimilar"];
    List<DISR190002Master> List_All_Tipe = (List<DISR190002Master>)ViewData["all_tipe"];
    //List<DISR190002Master> List_Printer = (List<DISR190002Master>)ViewData["printers"];
    List<string> List_Printer = (List<string>)ViewData["printers"];

}



<div class="col-md-4" style="position: relative;">
    <!------------------- Form Input Print ------------>
    <div class="col-xs-12 col-sm-12" style="overflow: hidden;" id="">
        <p style="width: 100%; color: blue; text-decoration: underline;">
            <b>Form Input</b>
        </p>
        <div class="col-xs-12 col-md-12" style="border: 1px solid #eaeaea; padding: 10px 5px 10px 5px;">
            <div class="col-md-12" style="">
                <div class="col-md-12">
                    <label>Pilih Printer : </label>
                    <div id="installedPrinters">
                        <select class="form-control select2" name="installedPrinterName" id="selectPrinter"></select>
                    </div>
                </div>
            </div>
            @*<div class="col-md-12" style="">
                    <div class="col-md-12">
                        <label>Pilih File : </label>
                        <input id="input-local-xls-file" type="file" />
                    </div>
                </div>*@
            <!--<div class="col-md-12" style="">
                <div class="col-md-12">
                    <div id="loadPrinters">
                        Click to load and select one of the installed printers!
                        <br />-->
            @*<input type="button" onclick="javascript:jsWebClientPrint.getPrinters();" value="Load installed printers..." />*@
            <!--<a onclick="javascript: jsWebClientPrint.getPrinters();" class="btn btn-success">Get Printers Info...</a>
                        <br /><br />
                    </div>
                    <div id="installedPrinters" style="visibility:hidden">
                        <label for="installedPrinterName">Select an installed Printer:</label>
                        <select name="installedPrinterName" id="installedPrinterName"></select>
                    </div>
                </div>
            </div>-->
            @*<div class="col-md-12" style="margin-top: 15px;">
                    <div class="col-md-12">
                        <label>Pilih Printer : </label>
                        <select class="form-control select2" id="selectPrinter">
                            <option value="" selected="selected" style="font-style: italic; color: gray"> --- Pilih Printer --- </option>
                            @{
                                if (List_Printer != null)
                                {
                                    if (List_Printer.Count() > 0)
                                    {
                                        foreach (string printer_name in List_Printer)
                                        {
                                            <option value="@printer_name">@printer_name</option>
                                        }
                                    }
                                }
                            }
                        </select>
                    </div>
                </div>*@
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-6">
                    <label>ID Produksi</label>
                    <input type="text" autocomplete="off" class="form-control" id="searchIDSeisan" oninput="eventAutoSearchTyping()" maxlength="20">
                </div>
                <div class="col-md-6">
                    <label>DMC TYPE</label>
                    <input autocomplete="off" id="editIDHinmoku" readonly type="text" class="form-control" />
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-6">
                    <input type="checkbox" id="checkSimilar" style="width:15px; height:15px; cursor:pointer"><label style="margin-left:5px">SIMILAR</label>
                    <input type="text" autocomplete="off" class="form-control" id="editSimilar">
                </div>
                <div class="col-md-6">
                    <input type="checkbox" id="checkSearchSimilar" style="width:15px; height:15px; cursor:pointer"><label style="margin-left:5px">Search Similar</label>
                    <select class="form-control select2" id="selectSimilarTipe">
                        <option value="" selected="selected" style="font-style: italic; color: gray"> --- Search --- </option>
                        @{
                            if (List_All_Tipe != null)
                            {
                                if (List_All_Tipe.Count() > 0)
                                {
                                    foreach (DISR190002Master item in List_All_Tipe)
                                    {
                                        <option value="@item.CODE">@item.CODE</option>
                                    }
                                }
                            }
                        }
                    </select>
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-6">
                    <label style="margin-top:5px">LOT</label>
                    <input id="editLot" required type="text" readonly class="form-control" />
                </div>
                <div class="col-md-6" id="Display_LotIhouse">
                    @*<input type="checkbox" id="checkLotIhouse" style="width:15px; height:15px; cursor:pointer">*@<label style="margin-left:5px">LOT IHOUSE</label>
                    <input type="text" autocomplete="off" class="form-control" id="editLotIhouse">
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-6">
                    <label>QTY/PACK</label>
                    <input autocomplete="off" id="editQty" required type="number" class="form-control" />
                </div>
                <div class="col-md-6">
                    <label>ADM IT</label>
                    <input type="text" autocomplete="off" class="form-control" id="editADM_IT">
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-6">
                    <label>OPT. PRESS/ SCRIBE</label>
                    <input autocomplete="off" id="editOpt_Press" required type="text" class="form-control" />
                </div>
                <div class="col-md-6">
                    <label>OPT. LAM/ PACKING</label>
                    <input autocomplete="off" id="editOpt_Lam" type="text" class="form-control">
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-6">
                    <label>JENIS TINTA DOT</label>
                    <input autocomplete="off" id="editJenisTintaDot" required type="text" class="form-control" />
                </div>
                <div class="col-md-6">
                    <label>QTY PRINT</label>
                    <input autocomplete="off" id="editQtyPrint" type="number" class="form-control">
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;" id="Display_GlassBuwon">
                <div class="col-md-6">
                    <input type="checkbox" id="editGlassBuwon">
                    <label>GLASS BUWON</label>
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;" id="Display_AfterLaminate">
                <div class="col-md-6">
                    <input type="checkbox" id="editAfterLaminate">
                    <label>AFTER LAMINATE</label>
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-6">
                    <input type="checkbox" class="chkboxname" id="editSpecialPrint">
                    <b><u>SPECIAL PRINT</u></b>
                </div>
            </div>
            <div class="col-md-12" style="margin-top: 15px;">
                <div class="col-md-12" id="display_special_print">
                    @*<input type="checkbox" class="chkboxname" id="checkPengganti">*@
                    <input type="radio" id="checkPengganti" name="chooseSpecialPrint">
                    <label for="checkPengganti">PENGGANTI</label>
                    <input id="editPengganti1" type="text" style="width:60px; height:25px"> /
                    <input id="editPengganti2" type="text" style="width:60px; height:25px">

                    <br />
                    <input type="radio" id="chekTambahan" name="chooseSpecialPrint">
                    <label for="chekTambahan">TAMBAHAN</label>
                    <input id="editTambahan1" type="number" style="width:60px; height:25px"> /
                    <input id="editTambahan2" type="number" style="width:60px; height:25px"> /
                    <input id="editTambahan3" type="text" style="width:60px; height:25px">
                </div>
            </div>

            <div class="col-md-12" style="margin-top: 20px">
                <div class="col-md-6" style="text-align: right">
                    <button type="button" onclick="clear_search()" class="btn btn-grey btn-sm" id="btn_clear" style="width: 80px;"><i class="fa fa-eraser"></i> Clear</button>
                </div>
                <div class="col-md-6" style="text-align: left">
                    <button onclick="print_check()" class="btn btn-primary btn-sm" style="width: 85px;"><i class="fa fa-print"></i> Print</button>
                    @*<button onclick="doPrinting()" class="btn btn-primary btn-sm" style="width: 85px;"><i class="fa fa-print"></i> Print_File</button>*@
                </div>

            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    //WebSocket settings (Get Printer)
    JSPM.JSPrintManager.auto_reconnect = true;
    JSPM.JSPrintManager.start();
    JSPM.JSPrintManager.WS.onStatusChanged = function () {
        //get client installed printers
        if (jspmWSStatus()) {
            //get client installed printers
            JSPM.JSPrintManager.getPrinters().then(function (printersList) {
                clientPrinters = printersList;
                var options = '';
                for (var i = 0; i < clientPrinters.length; i++) {
                    options += '<option>' + clientPrinters[i] + '</option>';
                }
                $('#selectPrinter').html(options);
            });
        }
    };

    //Check JSPM WebSocket status
    function jspmWSStatus() {
        if (JSPM.JSPrintManager.websocket_status == JSPM.WSStatus.Open)
            return true;
        else if (JSPM.JSPrintManager.websocket_status == JSPM.WSStatus.Closed) {
            console.warn('JSPrintManager (JSPM) is not installed or not running! Download JSPM Client App from https://neodynamic.com/downloads/jspm');
            return false;
        }
        else if (JSPM.JSPrintManager.websocket_status == JSPM.WSStatus.Blocked) {
            alert('JSPM has blocked this website!');
            return false;
        }
    }

    var wcppPingDelay_ms = 10000; //10 sec

    function wcppDetectOnSuccess() {
        //WCPP utility is installed at the client side
        //redirect to WebClientPrint sample page

        //TIP! You can get the WCPP Version installed through arguments[0]
        alert('Detected!');
        //window.location.href = "URL_TO_SAMPLE_PAGE";
    }

    function wcppDetectOnFailure() {
        //It seems WCPP is not installed at the client side
        //kindly ask the user to install it

        alert('No Detected!');
    }

    //var wcppGetPrintersTimeout_ms = 60000; //60 sec
    //var wcppGetPrintersTimeoutStep_ms = 500; //0.5 sec

    //function wcpGetPrintersOnSuccess() {
    //    // Display client installed printers
    //    if (arguments[0].length > 0) {
    //        var p = arguments[0].split("|");
    //        var options = '';
    //        for (var i = 0; i < p.length; i++) {
    //            options += '<option>' + p[i] + '</option>';
    //        }
    //        $('#installedPrinters').css('visibility', 'visible');
    //        $('#installedPrinterName').html(options);
    //        $('#installedPrinterName').focus();
    //        $('#loadPrinters').hide();
    //    } else {
    //        alert("No printers are installed in your system.");
    //    }
    //}
    //function wcpGetPrintersOnFailure() {
    //    // Do something if printers cannot be got from the client
    //    alert("No printers are installed in your system.");
    //}


</script>

@* WCPP detection script generated by HomeController *@
@Html.Raw(ViewBag.WCPPDetectionScript)
@Html.Raw(ViewBag.WCPScript);
@*@Html.Raw(Neodynamic.SDK.Web.WebClientPrint.CreateScript(Url.Action("PrintFile", "DISR190002", null, Request.Url.Scheme)))*@

@section scripts{
    @* WCPP detection script generated by HomeController *@
    @*@Html.Raw(ViewBag.WCPPDetectionScript)*@
    @*@Html.Raw(ViewBag.WCPScript);*@
    @*@Html.Raw(ViewData["WCPPDetectionScript"]);
        @Html.Raw(ViewData["WCPScript"]);*@
}